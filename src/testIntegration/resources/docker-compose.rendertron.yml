version: '2'

services:
  rendertron:
    image: ammobindotca/rendertron
    environment:
      - PORT=3005
    depends_on:
      - minimalwebapp
#    ports:
#      - "3005:3005"


  # Resourcen müssen via HTTP im selben Netzwerk bereit stehen
  minimalwebapp:
    image: nginx:alpine
    volumes:
      - ./minimalwebapp:/usr/share/nginx/html:ro
