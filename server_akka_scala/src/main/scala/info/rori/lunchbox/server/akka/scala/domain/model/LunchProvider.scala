package info.rori.lunchbox.server.akka.scala.domain.model

case class LunchProvider(
                          id: Id,
                          name: String,
                          location: Location
                          )
