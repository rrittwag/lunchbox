// ------------------------
//   Applikation starten
// ------------------------
bootRun {
  doFirst {
    //    args += '--logging.level.root=DEBUG'
    args += '--debug'
    args += "--external.ocr.server-url=http://localhost:9292"
  }

  debugOptions {
    enabled = true
    port = 5005
    server = true
    suspend = false
  }
}
